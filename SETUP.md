# วิธีตั้งค่าฐานข้อมูลถาวร (Google Sheets) 🗄️

เป้าหมาย: ทำให้ข้อมูลวัด/งานบุญที่บันทึก **อยู่ถาวร ไม่หายแม้อัปเดตโค้ดกี่เวอร์ชั่น**
และแชร์เหมือนกันทุกอุปกรณ์ (มือถือ/คอม/แท็บเล็ต)

> ทำครั้งเดียวจบ ใช้เวลา ~5 นาที — ไม่ต้องเขียนโค้ดเพิ่ม

---

## ทำไมต้องทำขั้นตอนนี้?

ก่อนหน้านี้ข้อมูลที่กดบันทึกถูกเก็บไว้ใน **เบราว์เซอร์เครื่องเดียว** (localStorage)
จึงรู้สึกเหมือน "ข้อมูลรีเซ็ต" เพราะ:
- เปิดอีกเครื่อง/มือถือ → ไม่เห็นข้อมูลที่แก้
- ล้างแคชเบราว์เซอร์ → ข้อมูลหาย

วิธีแก้ที่ถูกต้องคือเก็บข้อมูลไว้ที่ **ฐานข้อมูลกลาง (Google Sheets)**
ซึ่งอยู่นอกโค้ดเว็บ → อัปเดตโค้ดกี่ครั้งก็ไม่กระทบข้อมูล

---

## ขั้นตอน

### 1) สร้าง Google Sheet
- เข้า https://sheets.google.com → สร้างไฟล์เปล่าใหม่ 1 ไฟล์ (ตั้งชื่ออะไรก็ได้ เช่น “ฐานข้อมูลวัดยุโรป”)

### 2) เปิด Apps Script แล้ววางโค้ด
- ในชีต กดเมนู **Extensions → Apps Script**
- ลบโค้ดเดิมที่มีทั้งหมด แล้ว **คัดลอกเนื้อหาทั้งไฟล์ `apps-script.gs`** จากโปรเจกต์นี้มาวาง
- กดบันทึก (ไอคอน 💾)

### 3) ตั้งรหัสผ่านให้ตรงกัน
- ในโค้ด Apps Script บรรทัด `const SECRET = "dhamma2569";`
  ต้องตรงกับ `ADMIN_PASSWORD` ในไฟล์ `config.js`
  (ค่าเริ่มต้นตรงกันอยู่แล้ว — แนะนำให้เปลี่ยนเป็นรหัสใหม่ทั้งสองที่)

### 4) Deploy เป็นเว็บแอป
- กดปุ่ม **Deploy → New deployment**
- ช่อง “Select type” (ไอคอนเฟือง) เลือก **Web app**
- ตั้งค่า:
  - **Execute as:** `Me` (อีเมลคุณ)
  - **Who has access:** `Anyone`
- กด **Deploy** → ครั้งแรกระบบจะให้กด **Authorize access** และอนุญาตสิทธิ์ (เลือกบัญชี → Advanced → Go to … → Allow)
- คัดลอก **ลิงก์ที่ลงท้ายด้วย `/exec`**

### 5) วางลิงก์ลงในเว็บ
- เปิดไฟล์ `config.js` แก้บรรทัด:
  ```js
  DATA_URL: "",
  ```
  ให้เป็น (วางลิงก์ /exec ของคุณ):
  ```js
  DATA_URL: "https://script.google.com/macros/s/XXXXXXXX/exec",
  ```
- บันทึกไฟล์ แล้ว push ขึ้น GitHub ตามปกติ

### 6) อัปโหลดข้อมูลตั้งต้นขึ้นคลาวด์ (ครั้งแรกครั้งเดียว)
- เปิดเว็บ → กดปุ่มกุญแจมุมขวาบน → ใส่รหัสผ่าน เข้าหลังบ้าน
- ดูที่มุมล่างซ้ายของหลังบ้าน จะเห็น **“เชื่อมต่อฐานข้อมูลคลาวด์แล้ว”** (จุดสีเขียว)
- กดปุ่ม **“⤴ อัปโหลดทั้งหมด”** เพื่อส่งวัด 30 แห่งขึ้นชีตครั้งแรก
- เสร็จแล้ว! ต่อจากนี้ทุกครั้งที่เพิ่ม/แก้/ลบ ระบบจะบันทึกขึ้นคลาวด์ให้อัตโนมัติ

---

## หลังตั้งค่าเสร็จ จะเป็นอย่างไร?

| สถานะมุมล่างซ้ายหลังบ้าน | ความหมาย |
|---|---|
| 🟢 เชื่อมต่อฐานข้อมูลคลาวด์แล้ว | บันทึกขึ้นคลาวด์ ข้อมูลถาวร ✅ |
| 🟡 ยังไม่ได้ตั้งค่าคลาวด์ | ยังไม่ได้ใส่ `DATA_URL` (เก็บในเครื่องเท่านั้น) |
| 🔴 เชื่อมต่อคลาวด์ไม่สำเร็จ | เน็ตมีปัญหา/ลิงก์ผิด — ใช้ข้อมูลในเครื่องชั่วคราว |

- แก้ข้อมูลในชีต Google โดยตรงก็ได้ (แท็บ `Temples` และ `Events`)
- ถ้าเปลี่ยนโครงสร้าง/ดีพลอยใหม่ ให้ใช้ **Manage deployments → แก้ deployment เดิม** เพื่อให้ลิงก์ `/exec` คงเดิม

---

## หมายเหตุด้านความปลอดภัย
- รหัสผ่านแอดมินอยู่ในไฟล์ฝั่งหน้าเว็บ (อ่านได้จาก source) จึงเหมาะกับ “กันคนทั่วไปแก้เล่น” เท่านั้น
  ไม่ใช่ระบบความปลอดภัยระดับสูง — อย่าใช้รหัสซ้ำกับบัญชีสำคัญอื่น
- ถ้าต้องการความปลอดภัยจริงจังในอนาคต ควรย้ายไปใช้ระบบล็อกอินฝั่งเซิร์ฟเวอร์ (เช่น Firebase Auth)

---

# ✨ ฟีเจอร์ใหม่: ประวัติวัด, อายุวัด, อัปโหลดรูป, นำเข้า Excel, Dashboard, ส่งออก A4

## 🔴 สำคัญ: ต้อง Deploy Apps Script ใหม่ 1 ครั้ง (เพื่อให้ "อัปโหลดรูป" และคอลัมน์ใหม่ทำงาน)

มีการเพิ่ม **3 คอลัมน์ใหม่** (`established` วันสร้าง, `history` ประวัติ, `gallery` รูปภาพ)
และ **ระบบอัปโหลดรูปขึ้น Google Drive** ฟีเจอร์เหล่านี้จะใช้ได้ก็ต่อเมื่ออัปเดตโค้ดหลังบ้านก่อน:

1. เปิดชีต → **Extensions → Apps Script**
2. ลบโค้ดเดิมทั้งหมด แล้ววางเนื้อหาใหม่จากไฟล์ `apps-script.gs` (เวอร์ชันนี้) → กดบันทึก 💾
3. **⭐ อนุญาตสิทธิ์ Google Drive (ขั้นตอนสำคัญที่สุด — ห้ามข้าม):**
   - ที่แถบเครื่องมือด้านบน เลือกฟังก์ชัน **`authorizeDrive`** จากเมนูดรอปดาวน์ → กดปุ่ม **▶ Run**
   - หน้าต่างขออนุญาตจะเด้งขึ้น → **Review permissions** → เลือกบัญชี Google ของคุณ
   - ถ้าขึ้น “Google hasn’t verified this app” → กด **Advanced** → **Go to … (unsafe)**
   - กด **Allow** เพื่ออนุญาตการเข้าถึง **Google Drive**
   - ถ้ารันแล้วไม่ขึ้น error แดง = อนุญาตสำเร็จ (จะมีโฟลเดอร์ “DhammakayaEU Images” ใน Drive)
4. กด **Deploy → Manage deployments** → ดินสอ ✏️ (Edit) → **Version: New version** → **Deploy**
5. **ลิงก์ `/exec` จะคงเดิม** (ถ้าสร้าง deployment ใหม่ ให้นำลิงก์ `/exec` ใหม่ไปวางใน `config.js`)

> ⚠️ การ Deploy เฉย ๆ มัก**ไม่**ทริกเกอร์การขอสิทธิ์ Drive — จึงต้อง **Run `authorizeDrive` ก่อน** ตามข้อ 3 เสมอ
> อาการเมื่อยังไม่อนุญาต: กดอัปโหลดรูปแล้วขึ้น “อัปโหลดไม่สำเร็จ: ยังไม่ได้อนุญาตสิทธิ์ Google Drive”

> ระหว่างที่ยังไม่ Deploy ใหม่: เว็บใช้งานได้ปกติ แต่ปุ่ม "อัปโหลดรูป" จะยังไม่ทำงาน
> (ใช้ช่อง "วางลิงก์รูป (URL)" แทนได้ก่อน) และคอลัมน์ใหม่จะถูกเติมให้อัตโนมัติเมื่อบันทึกครั้งแรก

เมื่ออัปโหลดรูปสำเร็จ ระบบจะสร้างโฟลเดอร์ **“DhammakayaEU Images”** ใน Google Drive ของคุณ
และตั้งรูปเป็น “ผู้มีลิงก์ดูได้” โดยอัตโนมัติ

---

## 📜 หน้าประวัติวัด + อายุวัด
- ในหลังบ้าน แก้ไขวัด → กรอก **วันที่สร้าง/เปิดวัด**, **ประวัติความเป็นมา**, และ **อัปโหลดภาพวัด/กิจกรรม**
- หน้ารายละเอียดวัดจะโชว์ป้าย **“สร้างมาแล้ว X ปี Y เดือน Z วัน”** ใต้ชื่อวัด (คำนวณตามวันจริง)
- มีปุ่ม **“ประวัติและภาพวัด”** เปิดหน้า `temple.html` ที่รวมภาพ + ประวัติให้สาธุชนอ่าน

---

## 📊 Dashboard + นำเข้า Excel + ส่งออก A4
เข้าจากหลังบ้าน (ปุ่ม **“Dashboard & สถิติ”** หรือ **“นำเข้า / ส่งออก A4”** มุมล่างซ้าย)
หรือเปิด `dashboard.html` ตรง ๆ (ใส่รหัสแอดมิน)

**แท็บสถิติ:** สรุปจำนวนวัด/ประเทศ/พระ/งานบุญ, อายุเฉลี่ย, ความครบถ้วนข้อมูล, กราฟ →
ปุ่มส่งออกเป็น **PNG / JPG / PDF**

**แท็บนำเข้า:**
1. กด **ดาวน์โหลดไฟล์ตัวอย่าง (.xlsx)** (หรือ “ส่งออกข้อมูลปัจจุบัน” มาแก้)
2. กรอกใน Excel/Google Sheets → บันทึกเป็น `.xlsx` หรือ `.csv`
3. ลากไฟล์มาวาง → ตรวจตัวอย่าง → กด **ยืนยันนำเข้า**
   - แถวที่มี `id` ตรงวัดเดิม = อัปเดต / ไม่มี id = จับคู่ด้วยชื่อ หรือสร้างใหม่

**แท็บส่งออก A4:** เลือกรูปแบบ **“1 วัด/หน้า”** หรือ **“สมุดรวม”** →
กด “สร้างหน้าตัวอย่าง” → ดาวน์โหลด **PNG/JPG (.zip)** หรือ **PDF** ขนาด A4

> ระบบส่งออกทำงานในเบราว์เซอร์ทั้งหมด (ข้อมูลไม่ถูกส่งออกไปไหน)
> ต้องต่ออินเทอร์เน็ตครั้งแรกเพื่อโหลดเครื่องมือ และควรใช้บนคอมพิวเตอร์เพื่อความลื่นไหล
